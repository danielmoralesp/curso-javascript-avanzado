let base = 5
let height = 7

let triangleArea = (base, height) => base * height / 2

console.log(`El area de un triangulo de base ${base} y altura ${height} es: ${triangleArea(base, height)}`)

 // Ojo con la nueva sintaxis de es6, con la comilla y la interpolacion

const starWars7 = 'Starwars el despertar de la fuerza'
const pgStarWars7 = 13

const nameDaniel = 'Daniel'
const ageDaniel = 34

 
